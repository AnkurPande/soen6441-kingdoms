package controller;
/**
 * this class decides the playing strategy using strategy pattern and implements in various classes
 * @author TEAM B
 *
 */
public interface PlayingStrategy {
	public void selectAndMakeMove(GameController gc);
}

